---
name: cms-pptx-builder
description: 로컬 자료와 디자인 지침을 바탕으로 수업용 PPTX의 제작 계획, 승인 후 제작, 발표자 노트 작성, 전체 렌더링 검수가 필요한 요청에 사용합니다.
---

# CMS PPTX Builder

## 개요

자료에 근거한 수업용 PPTX를 계획부터 검수까지 일관되게 제작합니다. 대상 자료와 주제는 스킬에 고정하지 않고 매 요청의 입력값으로 받습니다.

**권장 하위 스킬:** `presentations:Presentations`를 우선 사용합니다. 해당 하위 스킬이나 기준 렌더러가 현재 환경에 없으면 작업을 중단하지 않고 [references/compatibility-mode.md](references/compatibility-mode.md)의 호환 모드로 전환합니다. 단, 제작 도구가 전혀 없거나 PPTX 구조 검증이 불가능하면 중단합니다.

## 입력 계약

작업 전에 다음 값을 확인합니다.

- 대상 자료의 경로
- 주제 또는 주제 선정을 위임한다는 명시적 지시
- 최종 PPTX 저장 경로
- 디자인 지침, 화면 비율, 글꼴, 발표자 노트 요구
- 웹 검색·외부 자료 사용 허용 여부

값을 임의로 고정하지 않습니다. 주제는 사용자가 직접 제공하거나 선정을 명시적으로 위임한 경우에만 정합니다. 웹 허용 여부가 없으면 로컬 입력만 사용합니다. 결과를 크게 바꾸는 다른 입력이 없고 자료에서도 안전하게 추론할 수 없으면 제작 전에 사용자에게 확인합니다.

## 작업 원칙

1. 원본은 읽기 전용으로 분석하고, 자료 밖의 사실·수치·출처를 만들지 않습니다.
2. 사용자가 계획을 먼저 요구했거나, 되돌리기 어려운 작업·둘 이상의 결과 파일 생성·여러 후보 중 일부를 사용자가 골라야 하는 작업이면 계획과 정확한 대상을 보여주고 현재 요청을 한 사용자의 승인을 받습니다. 승인 전에는 제작하지 않습니다.
3. 슬라이드 하나에는 핵심 메시지 하나만 둡니다. 텍스트, 표, 도식, 차트는 가능한 한 편집 가능한 개체로 만듭니다.
4. 수치 데이터가 없으면 정량 차트를 만들지 않습니다. 사용자가 시연용 데이터를 명시적으로 요청하지 않은 한 편집 가능한 개념도·비교표·흐름도로 전환합니다. 축·척도·수량 표식으로 값을 부호화하면 차트이고, 단계와 관계만 나타내면 비수치 도식입니다.
5. 제작 전에 `scripts/preflight.py`를 실행하거나 동등한 수동 점검을 수행합니다. 입력값·주제·출력 경로와 제작 도구, 렌더러, 글꼴을 확인하며 비어 있는 입력을 임의로 채우지 않습니다.
6. 지정 글꼴을 실제로 확인합니다. Pretendard가 없으면 운영체제에 실제 설치된 한글 지원 글꼴만 선택하고, 대체 글꼴명과 `확인 필요`를 기록합니다.
7. `presentations:Presentations`를 우선 사용하되, 부재 시 사용 가능한 로컬 제작·렌더링 도구로 계속합니다. 기준 렌더러, 실제 사용 렌더러, 미검증 엔진을 결과에 명시합니다.
8. `scripts/validate_pptx.py`를 실행하거나 동등한 구조 검증을 수행합니다. 구조 검증과 시각 렌더링 검수는 분리하며, 렌더링하지 못한 항목을 자동으로 통과 처리하지 않습니다.
9. 모든 슬라이드를 렌더링하여 잘림, 겹침, 대비, 간격과 가독성을 검수합니다. 검증하지 못한 항목은 완료로 단정하지 않습니다.
10. 판정 용어는 [references/validation-criteria.md](references/validation-criteria.md)의 `PASS`·`WARN`·`FAIL`을 따릅니다. PPTX 손상, 필수 발표자 노트 누락, 구조 검증 불능은 `FAIL`로 중단합니다.

## 상세 절차

계획, 디자인 변환, 발표자 노트, 검수와 완료 보고에는 [references/pptx-workflow.md](references/pptx-workflow.md)를 따릅니다. 환경 점검은 [references/environment-preflight.md](references/environment-preflight.md), 호환 모드는 [references/compatibility-mode.md](references/compatibility-mode.md), 판정 기준은 [references/validation-criteria.md](references/validation-criteria.md)를 사용합니다.

## 빠른 판단표

| 상황 | 판단 |
|---|---|
| 웹 사용이 금지됨 | 로컬 입력만 사용하고 URL을 추가하지 않음 |
| 웹 허용 여부가 없음 | 기본적으로 웹을 사용하지 않음 |
| 출력 파일이 이미 있음 | 자동 덮어쓰기·자동 이름 변경을 하지 않고 사용자에게 결정 요청 |
| 웹 px·CSS 규칙 | 16:9 좌표와 pt로 변환하고 부적합 규칙은 `확인 필요` |
| 수치 데이터가 없음 | 정량 차트 대신 편집 가능한 비수치 시각화 사용 |
| 글꼴을 확인할 수 없음 | 실제 설치된 한글 글꼴을 찾을 수 없으면 중단하고, 대체 글꼴을 쓰면 이름과 `확인 필요` 보고 |
| 기준 렌더러가 없음 | 가능한 로컬 렌더러로 계속하고 기준·실제·미검증 엔진을 보고 |
| 전체 렌더링을 못 함 | 시각 검수 완료라고 보고하지 않음 |
| 제작 도구가 전혀 없음 | `FAIL`로 중단하고 설치 또는 다른 환경을 요청 |

## 일반 오류

- 계획 경로를 실제 저장 경로처럼 보고하는 것
- 차트 이미지를 편집 가능한 차트로 간주하는 것
- 슬라이드 문구를 발표자 노트에 그대로 복사하는 것
- 출처가 없다는 이유로 `[Sources]`를 생략하는 것
- 파일이 열린다는 이유만으로 시각 검수까지 완료했다고 판단하는 것
